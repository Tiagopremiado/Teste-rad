import React from 'react';

/**
 * This component was identified as unused and potentially causing build issues.
 * It has been "neutralized" by replacing its content with a minimal, valid
 * component to prevent module resolution errors.
 */
const WelcomeScreen: React.FC = () => {
  return null;
};

export default WelcomeScreen;
