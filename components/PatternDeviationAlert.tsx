import React from 'react';

/**
 * This component has been deprecated and its functionality has been integrated
 * into the new NotificationBell component in the header. This centralizes all
 * system alerts into a single, unified notification center.
 */
const PatternDeviationAlert: React.FC = () => {
  return null;
};

export default PatternDeviationAlert;
