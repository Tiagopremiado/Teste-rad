import React from 'react';

/**
 * A component for monitoring API usage.
 * This component's visible part has been moved to a compact indicator in the Header.
 * This file is kept as a placeholder to avoid breaking potential module resolution,
 * but it no longer renders any UI.
 */
const ApiMonitor: React.FC = () => {
  return null;
};

export default ApiMonitor;
