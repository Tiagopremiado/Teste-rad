import React from 'react';

/**
 * The login page is no longer used. Access is now direct.
 * This file is kept to avoid breaking module imports.
 */
const LoginPage: React.FC = () => {
  return null;
};

export default LoginPage;
